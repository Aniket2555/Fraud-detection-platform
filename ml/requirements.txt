# ============================================================
# ML Training & Serving — requirements.txt
# Covers: ml/training/, ml/ensemble/, ml/serving/, ml/pipelines/
# Python 3.11 | Databricks Runtime 14.3 LTS compatible
# ============================================================

# --- Core ML Frameworks ---
xgboost>=2.0.3
torch>=2.2.0
torchvision>=0.17.0          # Needed by some torch data utilities

# --- Hyperparameter Optimisation ---
optuna>=3.6.0
optuna-integration>=3.6.0   # MLflow callback for Optuna

# --- Unsupervised / Classical ML ---
scikit-learn>=1.4.0          # IsolationForest, IsotonicRegression, LogisticRegression
imbalanced-learn>=0.12.0     # SMOTE data augmentation

# --- MLflow & Model Registry ---
mlflow>=2.13.0
mlflow[databricks]>=2.13.0   # Unity Catalog model registry support

# --- Data Processing ---
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0                # ks_2samp, jensenshannon for drift detection
pyarrow>=14.0.0              # Parquet / Delta read-write

# --- Explainability ---
shap>=0.45.0

# --- Score Calibration & Evaluation ---
joblib>=1.3.2

# --- Data Quality ---
pydeequ>=1.1.0               # PyDeequ quality gates on PySpark DataFrames

# --- Visualisation (training reports) ---
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Utilities ---
pyyaml>=6.0.1
python-dotenv>=1.0.0
tqdm>=4.66.0

# --- Testing ---
pytest>=8.1.0
pytest-cov>=5.0.0
