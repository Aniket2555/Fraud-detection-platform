# ============================================================
# Root requirements.txt — Local Development & CI/CD
# ============================================================
# This file installs all dependencies needed for:
#   - Running tests locally (pytest, unit + integration)
#   - Running the transaction producer locally
#   - Running linting and security scans in CI
#
# For component-specific requirements, see:
#   ml/requirements.txt                        ML training & serving
#   databricks/requirements.txt               Databricks cluster libraries
#   functions/decision_engine/requirements.txt Decision Engine Azure Function
#   functions/audit_logger/requirements.txt   Audit Logger Azure Function
#   producers/transaction_producer/requirements.txt Event producer
# ============================================================

# ── ML Training & Serving ────────────────────────────────────
xgboost>=2.0.3
torch>=2.2.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0          # SMOTE
optuna>=3.6.0
optuna-integration>=3.6.0
shap>=0.45.0
joblib>=1.3.2

# ── MLflow ───────────────────────────────────────────────────
mlflow>=2.13.0

# ── Data Processing ─────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0                     # Drift: ks_2samp, jensenshannon
pyarrow>=14.0.0
pydeequ>=1.1.0                    # Data quality gates

# ── Azure SDK ────────────────────────────────────────────────
azure-functions>=1.19.0
azure-eventhub>=5.11.7
azure-servicebus>=7.12.0
azure-appconfiguration>=1.5.0
azure-storage-file-datalake>=12.15.0
azure-identity>=1.16.0
azure-keyvault-secrets>=4.8.0
azure-mgmt-sql>=3.0.1             # Used by scripts/rotate_keyvault_secrets.py to update the live SQL admin password on rotation

# ── Explainability ───────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Schema Validation ────────────────────────────────────────
jsonschema>=4.22.0
pyyaml>=6.0.1

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.1.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# ── Linting & Code Quality ───────────────────────────────────
black>=24.4.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.9.0

# ── Security Scanning (also runs in CI) ──────────────────────
bandit>=1.7.8
pip-audit>=2.7.0
