# ============================================================
# Databricks Workspace Libraries — requirements.txt
# Install on cluster via: Compute → Libraries → Install New → PyPI
# Or via: %pip install -r /Workspace/fraud-detection/databricks/requirements.txt
# Databricks Runtime: 14.3 LTS ML (Python 3.11, Spark 3.5, MLflow 2.x pre-installed)
# ============================================================

# NOTE: The following packages are already pre-installed on DBR 14.3 LTS ML
# and do NOT need to be listed here:
#   - pyspark, delta-spark, mlflow, pandas, numpy, scikit-learn, matplotlib

# --- MLflow extras ---
mlflow[databricks]>=2.13.0      # Unity Catalog model registry integration

# --- ML Frameworks (not included in base DBR 14.3 LTS ML) ---
xgboost>=2.0.3
torch>=2.2.0                    # CPU build for notebook training; GPU variant installed by DBR ML

# --- Hyperparameter Optimisation ---
optuna>=3.6.0
optuna-integration>=3.6.0       # MLflow + Optuna integration

# --- Imbalanced data ---
imbalanced-learn>=0.12.0        # SMOTE

# --- Drift Detection ---
scipy>=1.12.0                   # ks_2samp, jensenshannon

# --- Explainability ---
shap>=0.45.0

# --- Data Quality ---
pydeequ>=1.1.0                  # PyDeequ quality gates

# --- Graph Features (GraphFrames installed separately as Maven package) ---
# Maven: graphframes:graphframes:0.8.3-spark3.5-s_2.12
# Add to cluster Advanced Options → Libraries → Maven

# --- Azure SDK for ADLS Gen2 write from Databricks ---
azure-storage-file-datalake>=12.15.0
azure-identity>=1.16.0

# --- Calibration & serialisation ---
joblib>=1.3.2

# --- Utilities ---
pyyaml>=6.0.1
python-dotenv>=1.0.0

# --- Testing (install in CI/CD, not on cluster) ---
pytest>=8.1.0
pytest-cov>=5.0.0
